{
  "name": "@gravity/core",
  "version": "0.1.0",
  "description": "Gravity game engine - platform-agnostic game logic",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run --testTimeout 10000",
    "test:coverage": "vitest run --coverage --testTimeout 10000",
    "lint": "eslint src --ext ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^5.3.0",
    "vitest": "^4.1.10"
  }
}
